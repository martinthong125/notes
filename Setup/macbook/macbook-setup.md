# Install all applications via brew

Copy the content of brew-casks.txt and brew-formulae.txt to local files with the same name

`xargs brew install < brew-casks.txt`

`xargs brew install < brew-formulae.txt`
